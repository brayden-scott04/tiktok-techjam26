# Action: draft

Propose a genuinely new direction, not a small tweak of an existing node. You've been given the list of directions already attempted (see the journal below) — pick a **direction not yet on that list**, or if none of the fixed enum values fit, use `"other"` and explain in `direction_note`.

Write the solution from scratch (you may reuse small utility code from a parent's file if it's genuinely reusable, e.g. a sigmoid function, but the modeling approach itself should be new). Base it on published methods where relevant and name them in `references`.

Be ambitious but realistic about `est_runtime_sec` — this will run inside a `max_seconds` budget, and a promising idea that never finishes is worth nothing.
